// 击退模式枚举
enum KnockbackMode
{
    Physics = 0,      // 物理模式：水平衰减 + 重力
    CustomCurve = 1,  // 曲线模式：完全自定义
    LinearDecay = 2   // 线性衰减：原有模式
}

// 受击类型枚举
enum HitType
{
    None,
    Light,
    Heavy,
    Launch
}
